<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MemberViewStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingStart">15dp</item>
        <item name="android:paddingEnd">15dp</item>
        <item name="android:paddingTop">10dp</item>
        <item name="android:paddingBottom">10dp</item>
        <item name="android:background">#ffffff</item>
        <item name="android:minHeight">50dp</item>
    </style>

    <style name="MemberViewWithIconStyle" parent="MemberViewStyle">
        <item name="coreIconRes">@mipmap/ic_launcher</item>
        <item name="coreIconWidth">35dp</item>
        <item name="coreIconHeight">35dp</item>
        <item name="coreTextMarginStart">10dp</item>
    </style>

</resources>
